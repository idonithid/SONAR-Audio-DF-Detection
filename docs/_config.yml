# .nojekyll is present in this folder, so GitHub Pages serves these files
# directly and does not invoke the Jekyll build pipeline. This file is kept
# only as a hint to readers; it has no effect when .nojekyll is present.
title: "SONAR — Audio Deepfake Detection (ICML 2026)"
description: "Spectral-Contrastive Audio Residuals for Generalizable Deepfake Detection"
