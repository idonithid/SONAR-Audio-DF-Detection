# SONAR demo (HuggingFace Spaces)
gradio>=4.0
plotly>=5.10
soundfile>=0.12
librosa>=0.10

# Pull the SONAR package + its model code straight from the GitHub repo.
# This brings in torch, fairseq, scipy, etc. via pyproject.toml.
git+https://github.com/idonithid/SONAR-Audio-DF-Detection.git

# Hub client for lazy checkpoint download (declared explicitly because
# huggingface_hub is not in the package's transitive deps yet).
huggingface_hub>=0.20
